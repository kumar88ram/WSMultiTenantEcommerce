<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MultiTenantEcommerce.Maui.ViewModels"
             xmlns:models="clr-namespace:MultiTenantEcommerce.Maui.Models"
             x:Class="MultiTenantEcommerce.Maui.Views.ProductListPage"
             x:DataType="viewModels:ProductListViewModel"
             Title="{Binding Title}">
    <ContentPage.Content>
        <CollectionView ItemsSource="{Binding Products}" SelectionMode="None" Padding="16">
            <CollectionView.EmptyView>
                <Label Text="No products found." HorizontalOptions="Center" VerticalOptions="Center" />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Product">
                    <Frame Padding="16" Margin="0,0,0,16" CornerRadius="16" HasShadow="False" BackgroundColor="White">
                        <Grid ColumnDefinitions="120,*" ColumnSpacing="12">
                            <Image Grid.Column="0" Source="{Binding ImageUrls[0]}" HeightRequest="100" Aspect="AspectFill" />
                            <VerticalStackLayout Grid.Column="1" Spacing="6">
                                <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="16" />
                                <Label Text="{Binding ShortDescription}" Style="{StaticResource SubtleLabelStyle}" />
                                <Label Text="{Binding Price, StringFormat='${0:F2}'}" FontAttributes="Bold" />
                                <Button Text="Details"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:ProductListViewModel}}, Path=ViewProductCommand}"
                                        CommandParameter="{Binding .}" />
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>
