<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MultiTenantEcommerce.Maui.ViewModels"
             xmlns:models="clr-namespace:MultiTenantEcommerce.Maui.Models"
             x:Class="MultiTenantEcommerce.Maui.Views.OrderHistoryPage"
             x:DataType="viewModels:OrderHistoryViewModel"
             Title="Orders">
    <CollectionView ItemsSource="{Binding Orders}" SelectionMode="None" Padding="16">
        <CollectionView.EmptyView>
            <Label Text="You have no orders yet." HorizontalOptions="Center" VerticalOptions="Center" />
        </CollectionView.EmptyView>
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:OrderSummary">
                <Frame Padding="16" Margin="0,0,0,12" CornerRadius="16" HasShadow="False" BackgroundColor="White">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="{Binding OrderId}" FontAttributes="Bold" />
                        <Label Text="{Binding OrderDate, StringFormat='Placed {0:MMM d, yyyy}'}" Style="{StaticResource SubtleLabelStyle}" />
                        <Label Text="{Binding Total, StringFormat='${0:F2}'}" />
                        <Label Text="{Binding Status}" />
                        <HorizontalStackLayout Spacing="12">
                            <Button Text="View"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:OrderHistoryViewModel}}, Path=ViewOrderCommand}"
                                    CommandParameter="{Binding .}" />
                            <Button Text="Refund"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:OrderHistoryViewModel}}, Path=RequestRefundCommand}"
                                    CommandParameter="{Binding .}" />
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
