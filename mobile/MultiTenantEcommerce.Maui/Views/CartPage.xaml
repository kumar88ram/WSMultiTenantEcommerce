<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MultiTenantEcommerce.Maui.ViewModels"
             xmlns:models="clr-namespace:MultiTenantEcommerce.Maui.Models"
             x:Class="MultiTenantEcommerce.Maui.Views.CartPage"
             x:DataType="viewModels:CartViewModel"
             Title="Cart">
    <ContentPage.Content>
        <Grid RowDefinitions="* Auto" BackgroundColor="{StaticResource BackgroundColor}">
            <CollectionView ItemsSource="{Binding Items}" SelectionMode="None" Padding="16">
                <CollectionView.EmptyView>
                    <Label Text="Your cart is empty." HorizontalOptions="Center" VerticalOptions="Center" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:CartItem">
                        <Frame Padding="16" Margin="0,0,0,12" CornerRadius="16" HasShadow="False" BackgroundColor="White">
                            <Grid ColumnDefinitions="Auto, *" ColumnSpacing="12">
                                <Image Source="{Binding Product.ImageUrls[0]}" HeightRequest="80" WidthRequest="80" Aspect="AspectFill" />
                                <VerticalStackLayout Grid.Column="1" Spacing="6">
                                    <Label Text="{Binding Product.Name}" FontAttributes="Bold" />
                                    <Label Text="{Binding Variant.SelectedOption}" Style="{StaticResource SubtleLabelStyle}" />
                                    <Grid ColumnDefinitions="Auto,Auto,Auto" ColumnSpacing="12" VerticalOptions="Center">
                                        <Button Text="-" Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:CartViewModel}}, Path=DecreaseQuantityCommand}" CommandParameter="{Binding .}" />
                                        <Label Grid.Column="1" Text="{Binding Quantity}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" />
                                        <Button Grid.Column="2" Text="+" Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:CartViewModel}}, Path=IncreaseQuantityCommand}" CommandParameter="{Binding .}" />
                                    </Grid>
                                    <Label Text="{Binding TotalPrice, StringFormat='${0:F2}'}" FontAttributes="Bold" />
                                    <Button Text="Remove" Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:CartViewModel}}, Path=RemoveItemCommand}" CommandParameter="{Binding .}" />
                                </VerticalStackLayout>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Frame Grid.Row="1" Padding="16" CornerRadius="24" HasShadow="True" BackgroundColor="White">
                <VerticalStackLayout Spacing="8">
                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="Subtotal" />
                        <Label Text="{Binding Subtotal, StringFormat='${0:F2}'}" />
                    </Grid>
                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="Taxes" />
                        <Label Text="{Binding Taxes, StringFormat='${0:F2}'}" />
                    </Grid>
                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="Total" FontAttributes="Bold" />
                        <Label Text="{Binding Total, StringFormat='${0:F2}'}" FontAttributes="Bold" />
                    </Grid>
                    <Button Text="Checkout" Command="{Binding CheckoutCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                </VerticalStackLayout>
            </Frame>
        </Grid>
    </ContentPage.Content>
</ContentPage>
