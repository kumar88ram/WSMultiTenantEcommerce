<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MultiTenantEcommerce.Maui.ViewModels"
             xmlns:models="clr-namespace:MultiTenantEcommerce.Maui.Models"
             xmlns:converters="clr-namespace:MultiTenantEcommerce.Maui.Converters"
             x:Class="MultiTenantEcommerce.Maui.Views.AddressBookPage"
             x:DataType="viewModels:AddressBookViewModel"
             Title="Addresses">
    <ContentPage.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </ContentPage.Resources>
    <CollectionView ItemsSource="{Binding Addresses}" SelectionMode="None" Padding="16">
        <CollectionView.EmptyView>
            <Label Text="No addresses saved." HorizontalOptions="Center" VerticalOptions="Center" />
        </CollectionView.EmptyView>
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:Address">
                <Frame Padding="16" Margin="0,0,0,12" CornerRadius="16" HasShadow="False" BackgroundColor="White">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="{Binding Label}" FontAttributes="Bold" />
                        <Label Text="{Binding Line1}" />
                        <Label Text="{Binding City}" />
                        <Label Text="{Binding PhoneNumber}" />
                        <Button Text="Set default"
                                IsEnabled="{Binding IsDefault, Converter={StaticResource InverseBooleanConverter}}"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:AddressBookViewModel}}, Path=SetDefaultCommand}"
                                CommandParameter="{Binding .}" />
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
