<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MultiTenantEcommerce.Maui.ViewModels"
             xmlns:models="clr-namespace:MultiTenantEcommerce.Maui.Models"
             x:Class="MultiTenantEcommerce.Maui.Views.WishlistPage"
             x:DataType="viewModels:WishlistViewModel"
             Title="Wishlist">
    <CollectionView ItemsSource="{Binding Items}" SelectionMode="None" Padding="16">
        <CollectionView.EmptyView>
            <Label Text="Your wishlist is empty." HorizontalOptions="Center" VerticalOptions="Center" />
        </CollectionView.EmptyView>
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:Product">
                <Frame Padding="16" Margin="0,0,0,12" CornerRadius="16" HasShadow="False" BackgroundColor="White">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                        <Image Source="{Binding ImageUrls[0]}" HeightRequest="80" WidthRequest="80" Aspect="AspectFill" />
                        <VerticalStackLayout Grid.Column="1" Spacing="4">
                            <Label Text="{Binding Name}" FontAttributes="Bold" />
                            <Label Text="{Binding ShortDescription}" Style="{StaticResource SubtleLabelStyle}" />
                            <Label Text="{Binding Price, StringFormat='${0:F2}'}" />
                            <Button Text="Move to cart"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:WishlistViewModel}}, Path=MoveToCartCommand}"
                                    CommandParameter="{Binding .}" />
                        </VerticalStackLayout>
                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
