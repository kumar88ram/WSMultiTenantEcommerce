<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MultiTenantEcommerce.Maui.ViewModels"
             xmlns:models="clr-namespace:MultiTenantEcommerce.Maui.Models"
             x:Class="MultiTenantEcommerce.Maui.Views.SupportTicketsPage"
             x:DataType="viewModels:SupportTicketsViewModel"
             Title="Support">
    <CollectionView ItemsSource="{Binding Tickets}" SelectionMode="None" Padding="16">
        <CollectionView.EmptyView>
            <Label Text="No tickets yet." HorizontalOptions="Center" VerticalOptions="Center" />
        </CollectionView.EmptyView>
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:SupportTicket">
                <Frame Padding="16" Margin="0,0,0,12" CornerRadius="16" HasShadow="False" BackgroundColor="White">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="{Binding Subject}" FontAttributes="Bold" />
                        <Label Text="{Binding Status}" Style="{StaticResource SubtleLabelStyle}" />
                        <Label Text="{Binding LastMessagePreview}" Style="{StaticResource SubtleLabelStyle}" />
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
